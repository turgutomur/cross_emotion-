"""
Training package (scaffold — full implementation in Week 2-3).

Planned modules:
    trainer.py     -- Main training loop (supports CE / Focal / DANN / CDAN)
    losses.py      -- Focal loss, class-balanced focal loss
    schedulers.py  -- DANN lambda sigmoid annealing
"""
